<h1>Wikinimous</h1>


<% @articles.each do |article| %>
  <h1><%= link_to article.title, article_path(article) %><%= link_to '❌', article_path(article), method: :delete, data: { confirm: 'Are you sure to delete?'} %></h1>

  <p><%= article.content.first(30) %></p>
<% end %>
